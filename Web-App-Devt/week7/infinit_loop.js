//and infinte loop is a condition that continues to execute indefinitely because the condition that controls the loops is always true or false
//examples include;";; meaning the condition will be true,i meaning the condition will never end"

//example of an infinite loop
for (let x = 0; ; x++){
    console.log(x);
}